# Build script for ch1_e1 on Linux (x86_64)
export RBDIR=$(pwd) # RBDIR = Root Build DIRectory

cd $RBDIR # Now we'll build ch1_e1
make -f makefile.linux x86_64_conv # Go!

printf "\n\nDone.\n" # Done.